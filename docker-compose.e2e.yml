services:
  alice:
    build: .
    volumes:
      - alice-data:/apoci/storage
      - ./configs/e2e-alice.yaml:/apoci/e2e.yaml:ro
    command: ["serve", "-c", "/apoci/e2e.yaml"]
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5000/healthz"]
      interval: 2s
      timeout: 3s
      retries: 10

  bob:
    build: .
    volumes:
      - bob-data:/apoci/storage
      - ./configs/e2e-bob.yaml:/apoci/e2e.yaml:ro
    command: ["serve", "-c", "/apoci/e2e.yaml"]
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5000/healthz"]
      interval: 2s
      timeout: 3s
      retries: 10

  e2e:
    image: alpine:3.23
    depends_on:
      alice:
        condition: service_healthy
      bob:
        condition: service_healthy
    volumes:
      - ./tests/e2e:/tests:ro
    entrypoint: ["sh", "-c", "apk add --no-cache curl coreutils > /dev/null 2>&1 && sh /tests/federation.sh"]

volumes:
  alice-data:
  bob-data:
